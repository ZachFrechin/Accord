{
  "db_name": "PostgreSQL",
  "query": "UPDATE email_outbox SET attempts = attempts + 1, last_error = $2, status = CASE WHEN attempts + 1 >= $3 THEN 'failed' ELSE 'pending' END, next_attempt_at = now() + make_interval(secs => LEAST(300, power(2, attempts + 1)::int)) WHERE id = $1",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Uuid",
        "Text",
        "Int4"
      ]
    },
    "nullable": []
  },
  "hash": "5b904f0bb0832c37eb971a122016db0db7f9474eac1201ab0014178426298158"
}
